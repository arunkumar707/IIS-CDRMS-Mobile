# =============================================================================
# SINGLE SOURCE OF TRUTH for the API base URL (no trailing /api).
# Loaded by app.config.js for Metro, EAS, and local APK builds.
# Do not duplicate this value in eas.json, config.ts, or .env.
# =============================================================================
EXPO_PUBLIC_API_URL=http://14.97.141.54:3710
